#!/bin/sh
# TeXShop engine script: acts as a drop-in pdflatex replacement.
# TeXShop invokes this as `./Secret-pdfLaTeX.engine document.tex` with the
# working directory already set to the document's directory.
exec secret-latex engine pdflatex -interaction=nonstopmode -synctex=1 "$@"
