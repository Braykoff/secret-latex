#!/bin/sh
# TeXShop engine script: acts as a drop-in xelatex replacement.
# TeXShop invokes this as `./Secret-XeLaTeX.engine document.tex` with the
# working directory already set to the document's directory.
exec secret-latex engine xelatex -interaction=nonstopmode -synctex=1 "$@"
