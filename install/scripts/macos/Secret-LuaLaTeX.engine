#!/bin/sh
# TeXShop engine script: acts as a drop-in lualatex replacement.
# TeXShop invokes this as `./Secret-LuaLaTeX.engine document.tex` with the
# working directory already set to the document's directory.
exec secret-latex engine lualatex -interaction=nonstopmode -synctex=1 "$@"
